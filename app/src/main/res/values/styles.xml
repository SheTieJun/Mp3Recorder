<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="BaseTheme.MD3">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="transparent_music_style" parent="transparent_dialog_fragment_style">
        <item name="behavior_peekHeight">373dp</item>
        <item name="android:windowAnimationStyle">@style/Animation.Design.BottomSheetDialog</item>
    </style>


    <!--transparent_dialog_fragment_style-->
    <style name="transparent_dialog_fragment_style" parent="@android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowBackground">@color/blackDivider</item>
    </style>

    <style name="App.SplashScreen" parent="Theme.SplashScreen.IconBackground">
        <item name="windowSplashScreenAnimatedIcon">@drawable/unsplash_cat</item>
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/white</item>
        <!--        must be set-->
        <item name="postSplashScreenTheme">@style/BaseTheme.MD3</item>
    </style>
</resources>
